# 本目录下随安装包分发的 .bat 一律以 GBK(CP936) 存储，不要改成 UTF-8。
#
# 原因：cmd.exe 解析批处理文件时按字节偏移逐行推进，而 `chcp 65001` 会在文件读到
# 一半时切换代码页——含非 ASCII 字符的行从此偏移错位，cmd 会把某一行的后半截当成
# 一条命令去执行。这不是显示问题：清理脚本里的 `set "ROAMING_DIR=..."` 就是这样被
# 劈断的，变量成了空字符串，用户确认"清理"之后一个文件都没删，且全程只报几条
# "不是内部或外部命令"。文件改用系统 ANSI(936) 编码 + `chcp 936` 后完全正常，
# 且从任意初始代码页启动都正常（已实测 936 / 437）。
#
# eol=crlf 是给 cmd 用的（它对纯 LF 的批处理文件行为不可靠）。GBK 的双字节序列里
# 不会出现 0x0D/0x0A，所以 git 的行尾归一化不会破坏编码。
*.bat text eol=crlf
