<Window x:Class="UEModManager.Views.ManagementCenterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:UEModManager.Localization"
        Title="{loc:Text &#x27;高级管理&#x27;}" Height="620" Width="520"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource CyberModalWindow}">

    <Window.CommandBindings>
        <CommandBinding Command="{x:Static SystemCommands.CloseWindowCommand}" Executed="OnCloseWindow"/>
    </Window.CommandBindings>

    <Grid>
        <Grid.RowDefinitions>
            <!-- 标题 -->
            <RowDefinition Height="Auto"/>
            <!-- Tab 栏 -->
            <RowDefinition Height="Auto"/>
            <!-- Tab 内容 -->
            <RowDefinition Height="*"/>
            <!-- 底部公共操作 -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ═══ 标题 ═══ -->
        <Border Grid.Row="0" Padding="24,20,24,12">
            <StackPanel Orientation="Horizontal">
                <Viewbox Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,10,0">
                    <Canvas Width="24" Height="24">
                        <Path Data="M20 7H12M20 7V3M20 7L16.5 10.5M4 17H12M4 17V21M4 17L7.5 13.5M20 17H12M20 17V21M20 17L16.5 13.5M4 7H12M4 7V3M4 7L7.5 10.5"
                              Stroke="{StaticResource PrimaryBrush}" StrokeThickness="2"
                              StrokeLineJoin="Round" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                    </Canvas>
                </Viewbox>
                <TextBlock Text="{loc:Text &#x27;高级管理&#x27;}" FontSize="17" FontWeight="SemiBold"
                           Foreground="{StaticResource Text100Brush}" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- ═══ Tab 栏 ═══ -->
        <Border Grid.Row="1" BorderBrush="{StaticResource CyberBorderBrush}" BorderThickness="0,0,0,1" Margin="24,0">
            <StackPanel x:Name="TabBar" Orientation="Horizontal">
                <Button x:Name="TabModLib" Content="{loc:Text &#x27;已导入 MOD&#x27;}" Tag="0" Click="Tab_Click"
                        Style="{StaticResource ManagementTabStyle}"/>
                <Button x:Name="TabDeployHistory" Content="{loc:Text &#x27;安装记录&#x27;}" Tag="1" Click="Tab_Click"
                        Style="{StaticResource ManagementTabStyle}" Margin="24,0,0,0"/>
                <Button x:Name="TabConfigMerge" Content="{loc:Text &#x27;配置文件合并&#x27;}" Tag="2" Click="Tab_Click"
                        Style="{StaticResource ManagementTabStyle}" Margin="24,0,0,0"/>
                <Button x:Name="TabGenFiles" Content="{loc:Text &#x27;临时文件&#x27;}" Tag="3" Click="Tab_Click"
                        Style="{StaticResource ManagementTabStyle}" Margin="24,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- ═══ Tab 内容区 ═══ -->
        <Grid Grid.Row="2">
            <!-- Tab 0: MOD 库 -->
            <Grid x:Name="PanelModLib">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 统计卡片 -->
                <Grid Grid.Row="0" Margin="24,16,24,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}"
                            BorderBrush="{StaticResource CyberBorderBrush}" BorderThickness="1"
                            CornerRadius="{StaticResource RadiusSm}" Padding="12,10">
                        <StackPanel>
                            <TextBlock x:Name="RepoTotalSize" Text="0 B" FontSize="16" FontWeight="Bold"
                                       Foreground="{StaticResource Text100Brush}"/>
                            <TextBlock Text="{loc:Text &#x27;总占用&#x27;}" FontSize="10" Foreground="{StaticResource Text500Brush}" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="2" Background="{StaticResource SurfaceBrush}"
                            BorderBrush="{StaticResource CyberBorderBrush}" BorderThickness="1"
                            CornerRadius="{StaticResource RadiusSm}" Padding="12,10">
                        <StackPanel>
                            <TextBlock x:Name="RepoTotalCount" Text="0" FontSize="16" FontWeight="Bold"
                                       Foreground="{StaticResource Text100Brush}"/>
                            <TextBlock Text="{loc:Text &#x27;MOD 数量&#x27;}" FontSize="10" Foreground="{StaticResource Text500Brush}" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="4" Background="{StaticResource SurfaceBrush}"
                            BorderBrush="{StaticResource CyberBorderBrush}" BorderThickness="1"
                            CornerRadius="{StaticResource RadiusSm}" Padding="12,10">
                        <StackPanel>
                            <TextBlock x:Name="RepoUnrefCount" Text="0" FontSize="16" FontWeight="Bold"
                                       Foreground="{StaticResource StatusOrangeBrush}"/>
                            <TextBlock Text="{loc:Text &#x27;未使用&#x27;}" FontSize="10" Foreground="{StaticResource Text500Brush}" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="6" Background="{StaticResource SurfaceBrush}"
                            BorderBrush="{StaticResource CyberBorderBrush}" BorderThickness="1"
                            CornerRadius="{StaticResource RadiusSm}" Padding="12,10">
                        <StackPanel>
                            <TextBlock x:Name="RepoDupCount" Text="0" FontSize="16" FontWeight="Bold"
                                       Foreground="{StaticResource StatusOrangeBrush}"/>
                            <TextBlock Text="{loc:Text &#x27;重复文件&#x27;}" FontSize="10" Foreground="{StaticResource Text500Brush}" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- 包列表 -->
                <Border Grid.Row="1" Margin="24,0" Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource CyberBorderBrush}" BorderThickness="1"
                        CornerRadius="{StaticResource RadiusSm}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="RepoPackageList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="16,10" BorderBrush="{StaticResource CyberBorderBrush}"
                                            BorderThickness="0,0,0,1">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding DisplayName}"
                                                       Foreground="{StaticResource Text200Brush}" FontSize="12"
                                                       VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Grid.Column="1" Text="{Binding StatusText}"
                                                       Foreground="{Binding StatusBrush}" FontSize="11"
                                                       VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>

                <!-- 底部操作 -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="24,12">
                    <Button Content="{loc:Text &#x27;检查缺失文件&#x27;}" Click="RepoCheckIntegrity_Click"
                            Style="{StaticResource CyberSecondaryButton}" Margin="0,0,8,0"/>
                    <Button Content="{loc:Text &#x27;合并相同文件&#x27;}" Click="RepoMergeDuplicates_Click"
                            Style="{StaticResource CyberSecondaryButton}" Margin="0,0,8,0"/>
                    <Button Content="{loc:Text &#x27;清理未使用&#x27;}" Click="RepoCleanUnreferenced_Click"
                            Style="{StaticResource CyberSecondaryButton}"/>
                </StackPanel>
            </Grid>

            <!-- Tab 1: 部署记录 -->
            <Grid x:Name="PanelDeployHistory" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Margin="24,16" Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource CyberBorderBrush}" BorderThickness="1"
                        CornerRadius="{StaticResource RadiusSm}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <Grid>
                            <ItemsControl x:Name="DeployHistoryList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Padding="16,12" BorderBrush="{StaticResource CyberBorderBrush}"
                                                BorderThickness="0,0,0,1">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding TimeText}"
                                                               Foreground="{StaticResource Text200Brush}" FontSize="12"/>
                                                    <TextBlock Text="{Binding SummaryText}"
                                                               Foreground="{StaticResource Text500Brush}" FontSize="11"
                                                               Margin="0,2,0,0"/>
                                                </StackPanel>
                                                <TextBlock Grid.Column="1" Text="{Binding StatusText}"
                                                           Foreground="{Binding StatusBrush}" FontSize="11"
                                                           VerticalAlignment="Center" Margin="12,0"/>
                                                <Button Grid.Column="2" Content="{loc:Text &#x27;回滚&#x27;}"
                                                        Style="{StaticResource CyberSecondaryButton}"
                                                        Visibility="{Binding RollbackVisibility}"
                                                        Tag="{Binding Transaction}"
                                                        Click="RollbackTransaction_Click"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <TextBlock x:Name="DeployHistoryEmptyText" Text="{loc:Text &#x27;暂无安装记录&#x27;}"
                                       Foreground="{StaticResource Text500Brush}" FontSize="13"
                                       HorizontalAlignment="Center" Margin="0,40,0,0"
                                       Visibility="Collapsed"/>
                        </Grid>
                    </ScrollViewer>
                </Border>
            </Grid>

            <!-- Tab 2: 配置合并 -->
            <Grid x:Name="PanelConfigMerge" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Margin="24,16" Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource CyberBorderBrush}" BorderThickness="1"
                        CornerRadius="{StaticResource RadiusSm}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="ConfigMergeList"/>
                    </ScrollViewer>
                </Border>
            </Grid>

            <!-- Tab 3: 生成文件 -->
            <Grid x:Name="PanelGenFiles" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 统计 -->
                <Grid Grid.Row="0" Margin="24,16,24,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}"
                            BorderBrush="{StaticResource CyberBorderBrush}" BorderThickness="1"
                            CornerRadius="{StaticResource RadiusSm}" Padding="12,10">
                        <StackPanel>
                            <TextBlock x:Name="GenActiveCount" Text="0" FontSize="16" FontWeight="Bold"
                                       Foreground="{StaticResource StatusGreenBrush}"/>
                            <TextBlock Text="{loc:Text &#x27;正在使用&#x27;}" FontSize="10" Foreground="{StaticResource Text500Brush}" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="2" Background="{StaticResource SurfaceBrush}"
                            BorderBrush="{StaticResource CyberBorderBrush}" BorderThickness="1"
                            CornerRadius="{StaticResource RadiusSm}" Padding="12,10">
                        <StackPanel>
                            <TextBlock x:Name="GenExpiredCount" Text="0" FontSize="16" FontWeight="Bold"
                                       Foreground="{StaticResource StatusOrangeBrush}"/>
                            <TextBlock Text="{loc:Text &#x27;可清理&#x27;}" FontSize="10" Foreground="{StaticResource Text500Brush}" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="4" Background="{StaticResource SurfaceBrush}"
                            BorderBrush="{StaticResource CyberBorderBrush}" BorderThickness="1"
                            CornerRadius="{StaticResource RadiusSm}" Padding="12,10">
                        <StackPanel>
                            <TextBlock x:Name="GenTotalSize" Text="0 B" FontSize="16" FontWeight="Bold"
                                       Foreground="{StaticResource Text100Brush}"/>
                            <TextBlock Text="{loc:Text &#x27;总占用&#x27;}" FontSize="10" Foreground="{StaticResource Text500Brush}" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- 列表 -->
                <Border Grid.Row="1" Margin="24,0" Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource CyberBorderBrush}" BorderThickness="1"
                        CornerRadius="{StaticResource RadiusSm}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <Grid>
                            <ItemsControl x:Name="GenFileList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Padding="16,10" BorderBrush="{StaticResource CyberBorderBrush}"
                                                BorderThickness="0,0,0,1">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding DisplayName}"
                                                               Foreground="{StaticResource Text200Brush}" FontSize="12"
                                                               TextTrimming="CharacterEllipsis"/>
                                                    <TextBlock Text="{Binding SummaryText}"
                                                               Foreground="{StaticResource Text500Brush}" FontSize="11"
                                                               Margin="0,2,0,0"/>
                                                </StackPanel>
                                                <TextBlock Grid.Column="1" Text="{Binding StatusText}"
                                                           Foreground="{Binding StatusBrush}" FontSize="11"
                                                           VerticalAlignment="Center" Margin="8,0,0,0"/>
                                                <Button Grid.Column="2" Content="{loc:Text &#x27;转为MOD&#x27;}"
                                                        Style="{StaticResource CyberSecondaryButton}"
                                                        Margin="4,0,0,0" Padding="8,2" FontSize="11"
                                                        Tag="{Binding ArtifactId}"
                                                        Click="GenPromote_Click"/>
                                                <Button Grid.Column="3" Content="{loc:Text &#x27;删除&#x27;}"
                                                        Style="{StaticResource CyberSecondaryButton}"
                                                        Margin="4,0,0,0" Padding="8,2" FontSize="11"
                                                        Tag="{Binding ArtifactId}"
                                                        Click="GenDelete_Click"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <TextBlock x:Name="GenFileEmptyText" Text="{loc:Text &#x27;暂无临时文件&#x27;}"
                                       Foreground="{StaticResource Text500Brush}" FontSize="13"
                                       HorizontalAlignment="Center" Margin="0,40,0,0"
                                       Visibility="Collapsed"/>
                        </Grid>
                    </ScrollViewer>
                </Border>

                <!-- 底部操作 -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="24,12">
                    <Button Content="{loc:Text &#x27;清理可删除文件&#x27;}" Click="GenCleanExpired_Click"
                            Style="{StaticResource CyberSecondaryButton}"/>
                </StackPanel>

                <!-- 注: 每行列表项自带"晋升"和"删除"按钮，见 code-behind AddGenFileRow 方法 -->
            </Grid>
        </Grid>

        <!-- ═══ 底部公共操作（Phase 11 诊断导出） ═══ -->
        <Border Grid.Row="3" Padding="24,10"
                BorderBrush="{StaticResource SurfaceBrush}" BorderThickness="0,1,0,0">
            <Grid>
                <TextBlock VerticalAlignment="Center" FontSize="12"
                           Foreground="{StaticResource Text500Brush}"
                           Text="{loc:Text &#x27;导出诊断包用于报 bug 时附带，含日志、数据快照、最近事务记录（敏感信息已脱敏）&#x27;}"/>
                <Button HorizontalAlignment="Right" Content="{loc:Text &#x27;导出诊断包&#x27;}"
                        Click="ExportDiagnostic_Click"
                        Style="{StaticResource CyberSecondaryButton}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
